@model IEnumerable<Organization.Application.Dtos.OrgUnit.OrgUnitDto>

@{
    ViewData["Title"] = "الوحدات التنظيمية";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0 text-dark"><i class="fas fa-sitemap text-secondary me-2"></i> الوحدات التنظيمية</h1>
            </div>
        </div>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        <div class="card card-outline card-primary">
            <div class="card-header">
                <h3 class="card-title">قائمة الوحدات التنظيمية</h3>
                <div class="card-tools">
                    <a asp-action="Create" class="btn btn-xs btn-primary"><i class="fas fa-plus"></i> إضافة وحدة</a>
                </div>
            </div>
            <div class="card-body p-0">
                <table class="table table-striped text-center">
                    <thead>
                        <tr>
                            <th>الكود</th>
                            <th>الاسم</th>
                            <th>النوع</th>
                            <th>الوحدة الأم</th>
                            <th>الحالة</th>
                            <th>الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Code</td>
                                <td>@item.Name</td>
                                @if (string.IsNullOrEmpty(item.OrgUnitTypeName))
                                {
                                    <td><span class="text-muted">-</span></td>
                                }
                                else
                                {
                                    <td>@item.OrgUnitTypeName</td>
                                }
                                
                                <td>@item.OrgUnitTypeName</td>
                                @if (string.IsNullOrEmpty(item.ParentName))
                                {
                                    <td><span class="text-muted">-</span></td>
                                }
                                else
                                {
                                    <td>@item.ParentName</td>
                                }
                                
                                <td>@item.ParentName</td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-success">نشط</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">غير نشط</span>
                                    }
                                </td>
                                <td>
                                   
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-xs btn-warning"><i class="fas fa-edit"></i></a>
                                    <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline">
                                        <button type="submit" class="btn btn-xs btn-danger" onclick="return confirm('هل أنت متأكد من الحذف؟')"><i class="fas fa-trash"></i></button>
                                    </form>
                                </td>
                            </tr>
                        }
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="6" class="text-center text-muted">لا توجد بيانات</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>